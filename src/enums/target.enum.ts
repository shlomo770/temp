export enum TargetState {
    active = 0,
    allocated = 1,
    designated = 2,
    track = 3,
    arm = 4,
    destroyed = 5,
}

export enum TargetStateString {
    active = "active",
    allocated = "allocated",
    designated = "designated",
    track = "track",
    arm = "arm",
    destroyed = "destroyed",
}

export enum TargetType {
    airplaneLarge = 0,
    airplaneMedium = 1,
    droneLarge = 2,
    droneMedium = 3,
}

